# =============================================================================
# Pet Roast AI — Full stack requirements
# Python 3.11 | Ubuntu 22.04 | TensorFlow 2.16
# =============================================================================

# ---------------------------------------------------------------------------
# Core API
# ---------------------------------------------------------------------------
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-dotenv==1.0.0
pydantic==2.7.1
pydantic-settings==2.2.1
httpx==0.27.2

# ---------------------------------------------------------------------------
# AI / LLM  (no Gemini — replaced by OpenAI)
# ---------------------------------------------------------------------------
openai==1.35.10
fal-client==0.4.1

# ---------------------------------------------------------------------------
# Pet Emotion Recognition — TF2 / Keras
# ---------------------------------------------------------------------------
tensorflow==2.16.2              # Ubuntu + Python 3.11 compatible wheel
# TF 2.16 ships keras as a separate package (keras 3)
keras==3.3.3

# ---------------------------------------------------------------------------
# Computer Vision
# ---------------------------------------------------------------------------
numpy>=1.24.0,<2.0              # TF 2.16 requires numpy < 2.0
opencv-python-headless>=4.9.0  # headless: no GUI/X11 dependency on server
Pillow>=10.0.0                  # used by Keras image utils
mediapipe>=0.10.0               # 46-point landmark detection (FaceMesh fallback)

# ---------------------------------------------------------------------------
# Media Processing
# ---------------------------------------------------------------------------
moviepy==1.0.3
ffmpeg-python==0.2.0
edge-tts==6.1.6
faster-whisper==1.0.2

# ---------------------------------------------------------------------------
# Async / Queue
# ---------------------------------------------------------------------------
arq>=0.25.0
redis>=4.5.0
aiofiles==24.1.0

# ---------------------------------------------------------------------------
# Utilities
# ---------------------------------------------------------------------------
tenacity==9.0.0
